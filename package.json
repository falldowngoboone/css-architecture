{
  "name": "css-architecture",
  "repository": {
    "type": "git"
  },
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "npm run clean",
    "build": "npm-run-all build:*",
    "build:styles": "sass --load-path=styles --style=compressed styles:build/styles",
    "clean": "rm -rf ./build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": {
    "name": "Ryan Boone",
    "email": "falldowngoboone@gmail.com",
    "url": "https://www.falldowngoboone.com"
  },
  "license": "ISC",
  "dependencies": {
    "sass": "^1.37.5"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}